import { t as pages } from "./gttContent-Cozi9HDi.js";
import { createFileRoute, lazyRouteComponent } from "@tanstack/react-router";
//#region src/routes/$slug.tsx
var $$splitComponentImporter = () => import("./_slug-CPrKLd5K.js");
var Route = createFileRoute("/$slug")({
	head: ({ params }) => {
		const { slug } = params;
		const page = pages.find((p) => p.slug === slug);
		if (!page) return { meta: [{ title: "Page not found | GTT-Method" }, {
			name: "description",
			content: "Page not found"
		}] };
		const title = page.titleEn;
		const description = page.descriptionEn;
		return { meta: [
			{ title: `${title} | GTT-Method` },
			{
				name: "description",
				content: description
			},
			{
				property: "og:title",
				content: `${title} | GTT-Method`
			},
			{
				property: "og:description",
				content: description
			}
		] };
	},
	component: lazyRouteComponent($$splitComponentImporter, "component")
});
//#endregion
export { Route as t };
